import React from 'react'

export default function UserEdit({userID}) {

    console.log(userID);

  return (
    <div>
        <button>sa</button>
        <label>sa</label>
        <label>{userID}</label>
        <button>as</button>

    </div>
  )
}
